import styled from "styled-components";

const Father = styled.div``;

const Child = styled.div``;

function Test() {
  return (
    <Father>
      <Child></Child>
    </Father>
  );
}

export default Test;
